source 'http://rubygems.org'

ruby '2.1.3'

gem 'capistrano', '~>2'
gem 'rails', '~>3.0'
#gem 'sqlite3'
#gem 'activerecord-mysql2-adapter'
gem 'devise'

# even though we don't use it... fuckers are forcing me to use pg.
gem 'pg'

gem 'rails_12factor'

group :development do
end

group :development, :test do
  gem 'rspec-rails'
  gem 'factory_girl_rails'
end
